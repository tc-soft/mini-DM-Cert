# Path to the SQLite database file. Defaults to ./data/mini-dm.db if unset.
DATABASE_PATH=./data/mini-dm.db

# Seed values for the default admin account created on first boot.
# If ADMIN_PASSWORD is left unset, a random password is generated and
# printed once to the server console on first run.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=###
